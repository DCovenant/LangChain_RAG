FROM nginx:alpine

# Copy both configurations
COPY webapp/nginx.conf /etc/nginx/nginx.conf.enabled
COPY webapp/nginx-disabled.conf /etc/nginx/nginx.conf.disabled

# Start with enabled by default
COPY webapp/nginx.conf /etc/nginx/nginx.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
